import React from 'react'

const usersEndpoint = `https://jsonplaceholder.typicode.com/users`;

// 1. Now, using the list component you built in 02.tsx, create a component that:
// 1.1 Fetches users from the usersEndpoint
// 1.2 Renders the list of users
// 1.3 Render the new component on the main page

// Consider:
// What steps aren't specified in the requirements, but are necessary to make it work?
// Does the API data match what the component expects?